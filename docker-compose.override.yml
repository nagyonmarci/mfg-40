# Lokális fejlesztési felülírások (NEM commitolni production-ba)
services:
  mfg-directus:
    environment:
      CORS_ORIGIN: "http://localhost:3000"
      PUBLIC_URL: "http://localhost:8055"
    ports:
      - "8056:8055"

networks:
  traefik_proxy:
    external: false
